{
  "name": "codemelt",
  "private": true,
  "license": "MIT",
  "description": "Repository intelligence and structured context exports for modern codebases.",
  "keywords": [
    "repository",
    "intelligence",
    "context",
    "optimization",
    "llm",
    "ai",
    "context-window",
    "cli"
  ],
  "homepage": "https://github.com/Vinay-Krishna-G/codemelt#readme",
  "bugs": {
    "url": "https://github.com/Vinay-Krishna-G/codemelt/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Vinay-Krishna-G/codemelt.git"
  },
  "scripts": {
    "clean": "rm -rf packages/*/dist apps/web/.next packages/*/*.tsbuildinfo tsconfig.tsbuildinfo",
    "clean:full": "rm -rf node_modules packages/*/dist apps/web/.next packages/*/*.tsbuildinfo tsconfig.tsbuildinfo",
    "dev": "npm run dev --workspace=codemelt-web",
    "build": "npm run build --workspace=codemelt-shared && npm run build --workspace=codemelt-core && npm run build --workspace=codemelt && npm run build --workspace=codemelt-web",
    "typecheck": "tsc -b",
    "test": "npx tsx --test packages/core/tests/*.ts",
    "lint": "npm run lint --workspace=codemelt-web"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=18"
  }
}